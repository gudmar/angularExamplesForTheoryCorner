import { MoveSliderDirective } from './move-slider.directive';

describe('MoveSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveSliderDirective();
    expect(directive).toBeTruthy();
  });
});
